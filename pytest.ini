[pytest]
markers =
    parity: tests comparing new engine output against golden captures of the old engine (see tests/golden/)
    slow: tests that are slow and/or cost real API calls; excluded from the fast loop via `pytest -m "not slow"`
